import { Emoji } from '@brillout/docpress'
import { PrerenderPreview } from './PrerenderPreview'
import ServerMiddleware from './ServerMiddleware.mdx'

### 只是个中间件

从服务端的角度来看，`vite-plugin-ssr` 只是个中间件

<ServerMiddleware />

### 预渲染 (aka SSG)

你也可以预渲染应用程序来消除生产环境对 Node.js 服务的需求，并且部署到任何静态主机
（Cloudflare Pages, GitHub Pages, Netlify, ...）

<PrerenderPreview />

<h3>Cloudflare Workers + <code>vite-plugin-ssr</code> = <Emoji name="red-heart" /></h3>

得益于 `vite-plugin-ssr` 的精简架构，你的应用程序不再受 Cloudflare Workers 的 1MB 包大小限制

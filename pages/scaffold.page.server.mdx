通过脚手架初始化 Vite + `vite-plugin-ssr` 应用：

```shell
npm init vite-plugin-ssr@latest
```

一旦应用程序创建成功，你可以使用任何包管理器（npm、Yarn、pnpm 等）

提示会让你选择：

 - `react`: React + JavaScript
 - `react-ts`: React + TypeScript
 - `vue`: Vue + JavaScript
 - `vue-ts`: Vue + TypeScript

选项:
 - `--skip-git`: 跳过初始化 Git 仓库

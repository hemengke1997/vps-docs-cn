import { Link } from '@brillout/docpress'

要定义一个 SPA 页面，我们在 `.page.client.js` 中 `export { Page }` 而非 `.page.js` 中。请参考：

- <Link href='/Page' />.
- <Link href='/render-modes#spa' />.

> 这对 SFC 组件也生效，例如 `.page.client.vue`

import {
  StaticHostDocIntro,
  StaticHostDocOutro,
} from "../components/static-host";
import { RepoLink } from "@brillout/docpress";

<StaticHostDocIntro
  staticHostLink={
    <>
      ，例如 <a href='https://pages.github.com/'>GitHub Pages</a>
    </>
  }
/>

我们添加一个空文件 `.nojekyll` 来避免[Github Pages 忽略以`_`开头的文件](https://stackoverflow.com/questions/6397780/names-starting-with-underscore-shows-errors-page-doesnot-exists-for-gh-pages-bra)，这会破坏 `_default.page.client.js` 等文件

我们推荐使用 [GitHub Pages Deploy Action](https://github.com/JamesIves/github-pages-deploy-action)，例如：

- `vite-plugin-ssr.com` 自动部署到 GitHub Pages 的 GitHub Action： <RepoLink path='.github/workflows/docs.yml' />

<StaticHostDocOutro
  baseUrlAddendum={
    <>
      {" "}
      例如，如果我们使用 Github Pages 的默认部署，我们需要这样做：<code>
        <i>username</i>.github.io/<i>repo-name/*</i>
      </code>.
    </>
  }
/>

import { RepoLink, Link } from "@brillout/docpress";

我们可以使用任意的路由库，如 Vue Router 或 React Router

浅层集成 （React/Vue Router 取代 `vite-plugin-ssr` 的内置路由器）

- <RepoLink path='/examples/vue-router/' />
- <RepoLink path='/examples/react-router/' />

深度集成 （React/Vue Router 与 `vite-plugin-ssr` 内置路由器协同工作）

- Vue Router：[work-in-progress](https://github.com/brillout/vite-plugin-ssr/issues/9#issuecomment-937477110)
- React Router：应该使用 [`onBeforeRoute()` hook](/onBeforeRoute)

我们建议只使用 vite-plugin-ssr 的内置路由器：它从根本上更简单

> 在做 SSR 的时候，不需要 React Router 或 Vue Router 等复杂的路由库

请注意，`vite-plugin-ssr` 的内置路由器支持：

- <Link text='客户端路由' href='/client-routing' />
- <Link text='嵌套布局' href='/layouts#nested-layouts' />

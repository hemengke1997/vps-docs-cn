import { Link } from '@brillout/docpress'

要定义纯 HTML 页面，我们需要在 `.page.server.js` 而不是 `.page.js` 中 `export { Page }`。请参考：

- <Link href='/Page' />.
- <Link href='/render-modes#html-only' />.

> 这也适用于 SFC 组件，例如 `.page.server.vue`
